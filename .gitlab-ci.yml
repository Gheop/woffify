stages:
  - test
  - build

test:
  stage: test
  image: golang:1.26-bookworm@sha256:9fdc884aacc3bec89b20ffc69f4bb369c78210e3e4f600387b5128b12c199f81
  variables:
    WOFF2_TAG: v1.0.2
    WOFF2_SHA: 1bccf208bca986e53a647dfe4811322adb06ecf8
  script:
    - apt-get update
    - apt-get install -y libharfbuzz-dev libbrotli-dev pkg-config g++ git
    # Debian/Ubuntu ship no woff2 -dev package; build the encoder from a pinned source.
    - git clone --depth 1 --branch "$WOFF2_TAG" https://github.com/google/woff2 /tmp/woff2
    - test "$(git -C /tmp/woff2 rev-parse HEAD)" = "$WOFF2_SHA"
    - git -C /tmp/woff2 apply "$CI_PROJECT_DIR/patches/woff2-monotonic-endpts.patch"
    - cd /tmp/woff2
    - >
      g++ -O2 -std=c++11 -include cstdint -fPIC -Iinclude -c
      src/woff2_enc.cc src/font.cc src/glyph.cc src/normalize.cc
      src/transform.cc src/table_tags.cc src/variable_length.cc
      src/woff2_common.cc src/woff2_out.cc
    - ar rcs /usr/lib/libwoff2enc.a *.o
    - cp -r include/woff2 /usr/include/
    - printf 'Name:\x20woff2enc\nDescription:\x20woff2\x20encoder\nVersion:\x201.0.2\nLibs:\x20-lwoff2enc\x20-lbrotlienc\x20-lbrotlicommon\nCflags:\n' > /usr/lib/pkgconfig/libwoff2enc.pc
    - cd "$CI_PROJECT_DIR"
    - go vet ./...
    - go test ./...
    - go build -o woffify .

build-image:
  stage: build
  image: docker:27
  services:
    - docker:27-dind
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_TAG =~ /^v/'
  variables:
    IMAGE_TAG: latest
  script:
    - if [ -n "$CI_COMMIT_TAG" ]; then IMAGE_TAG="$CI_COMMIT_TAG"; fi
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
    # Layer cache kept in the registry: HarfBuzz is only rebuilt when the
    # Dockerfile changes, not on every push.
    - docker buildx create --use --driver docker-container
    - >
      docker buildx build --push
      --cache-from "type=registry,ref=$CI_REGISTRY_IMAGE:buildcache"
      --cache-to "type=registry,ref=$CI_REGISTRY_IMAGE:buildcache,mode=max,image-manifest=true,oci-mediatypes=true"
      -t "$CI_REGISTRY_IMAGE:$IMAGE_TAG" .
